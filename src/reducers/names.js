import initialState from '../initialState'

const namesByIdReducer = (state=initialState.namesById, action) => {
	switch(action.type) {
		default:
			return state
	}
}

export default namesByIdReducer